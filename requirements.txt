# Core dependencies for GenAI Platform SDK
grpcio>=1.60.0
grpcio-tools>=1.60.0
protobuf>=4.25.0
openai
anthropic

# Optional dependencies for PostgreSQL (recommended for production)
# psycopg[binary]>=3.3.0  # Uncomment for PostgreSQL support
# or: pip install "psycopg[binary]"

# Development dependencies (optional)
# Install with: pip install -r requirements-dev.txt
# Or use: pip install -e ".[dev]"

